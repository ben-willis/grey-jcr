var Election = require('../../models/election');
var db = require('../../helpers/db');

var expect = require("chai").expect;

describe('Static Election Methods', function() {
    it("should create new elections");
    it("should delete elections");
});

describe('Election Object', function() {
    it("should add positions");
    it("should remove positions");

    it("should add nominees");
    it("should remove nominees");

    it("should cast votes");
    it("should check votes");
})
