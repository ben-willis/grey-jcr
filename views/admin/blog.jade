- var title = 'Home'

doctype html
html
	head
		include ./head.jade
	body
		div(class='ui container')
			include ./header.jade
			include ./menu.jade
			h2 Blog
			div(class="ui vertical segment")
				h3 Write a New Blog Post
				form(class="ui form" method="POST" action="/admin/blog/new")
					div(class="field")
						label Title
						input(type="text" name="title")
					div(class="field")
						label Message
						textarea(rows="5" name="message" id="editor1")
					div(class="field")
						label Post As
						select(name="position")
							each position in user.positions
								option(value=position.id)= position.title
					button(type="submit" class="ui submit button")
						i(class="write icon")
						| Create

			div(class="ui vertical segment")
				h3 Previous Blog Posts

				table(class="ui fixed table")
					thead
						tr
							th Title
							th Position
							th Date
							th Actions
					tbody
						each post in posts
							tr
								td= post.title
								td= post.position
								td= post.timestamp
								td
									a(class="ui edit button" href="/admin/blog/"+post.id+"/edit")
										i(class="edit icon")
										| Edit
									a(class="ui negative button" href="/admin/blog/"+post.id+"/delete" onclick="return confirm('Are you sure you want to delete this post?');")
										i(class="remove icon")
										| Delete

		script.
			CKEDITOR.replace( 'editor1' );