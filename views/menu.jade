div(class='ui stackable red inverted menu')
	a(class=(current=="home")?'active item':'item' href='/') Home
	a(class=(current=="jcr")?'active item':'item' href='/jcr') The JCR
	a(class=(current=="prospective")?'active item':'item' href='/prospective') Prospective Students
	a(class=(current=="support")?'active item':'item' href='/support') Support
	a(class=(current=="welfare")?'active item':'item' href='/welfare') Welfare
	a(class=(current=="sportsandsocs")?'active item':'item' href='/sportsandsocs') Sports & Societies
	a(class=(current=="events")?'active item':'item' href='/events') Events
	a(class=(current=="facilities")?'active item':'item' href='/facilities') Facilities
	a(class=(current=="mcr")?'active item':'item' href='/mcr') The MCR

	div(class="right menu")
		div(class="ui right aligned category search item")
			div(class="ui inverted transparent icon input")
				input(class="prompt" type="text" placeholder="Search...")
				i(class="search icon")
		div(class="results")

		script.
			$('.ui.search').search({
				apiSettings: {
					url: '/api/search?q={query}',
					onResponse: function(response) {
						$.each(response.results, function (key, value) {
							console.log(response)
							response.results[key].results.splice(5)
							console.log(response)
						})
						return response;
					}
				},
				type: 'category',
			});