var Booking = require('../../models/booking');
var db = require('../../helpers/db');

var expect = require("chai").expect;

describe("Booking mode", function() {
    it("should find by id");
    it("should find by booked for user and ticket id");
    it("should get all booked for or by a user");
    it("should count all by ticket id");
    it("should get all by event id");

    it("should create a new booking");

    it("should set notes for booking");

    it("should get choices for a booking");
    it("should set choices for a booking");
});